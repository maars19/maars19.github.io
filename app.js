document.addEventListener("DOMContentLoaded" , () => {
    const translations
    en
   <script>
   function changeLanguage(selectObj) {
       var selectedLanguage = selectObj.value;
       // Redirect to the selected language version of the website
       window.location.href = selectedLanguage;
   }
</script>
}